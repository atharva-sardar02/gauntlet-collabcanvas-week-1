{
  "rules": {
    "sessions": {
      "global-canvas-v1": {
        ".read": "auth != null",
        "$userId": {
          ".write": "auth != null && auth.uid == $userId"
        }
      }
    }
  }
}

