{
  "rules": {
    "sessions": {
      "global-canvas-v1": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid == $userId"
        }
      }
    }
  }
}

